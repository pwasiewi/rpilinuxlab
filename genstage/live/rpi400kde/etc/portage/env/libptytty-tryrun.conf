# cmake try_run(TTY_GID_SUPPORT) cannot execute the target test binary under
# cross — seed its answer: on any standard glibc+devpts Linux (the Pi
# included) ttys are group "tty", i.e. the test exits 0.
MYCMAKEARGS="-DTTY_GID_SUPPORT_EXITCODE=0"
