# Locales generated on the card. locale-gen cannot run during the cross build
# (host tool, target arch) — run it once via 'sudo xp rpi400kde enter' before
# 'image', or over ssh on first boot.
en_US.UTF-8 UTF-8
pl_PL.UTF-8 UTF-8
