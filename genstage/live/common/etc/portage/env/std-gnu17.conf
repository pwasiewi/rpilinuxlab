# GCC 15+ defaults to C23, which turns K&R-era constructs into hard errors
# (incompatible signal-handler pointer types, implicit int). For museum-piece
# C that upstream will never modernise, pin the previous standard.
CFLAGS="${CFLAGS} -std=gnu17"
