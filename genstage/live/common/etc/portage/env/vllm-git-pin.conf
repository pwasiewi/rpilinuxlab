# Pin dev-python/vllm-9999 to ONE vllm.git commit. Same reasoning as
# torch-git-pin.conf: a live ebuild resolves "newest main" at every unpack,
# and vllm's ~pins (flashinfer/tilelang/humming/cutlass-dsl) are only known to
# match a given tree. Bump together with the torch pin after a drift study.
#
# 2026-08-30: [CI] Restore gpu_1_queue routing for torch-abi audit (#54468)
# paired with torch e6fbf0c7602 (2026-09-06); flashinfer 0.6.12 -> 0.6.17.
EGIT_COMMIT="7a100bb617471801ee1d5525bfbb8fb238a345ea"
