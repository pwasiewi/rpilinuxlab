# dev-python/xgrammar 0.2.5 adds "<sci-ml/transformers-5" (upstream pyproject
# pin: v5 breaks TokenizerInfo.from_huggingface for several models). On this
# system that is a REGRESSION, not a fix - transformers-5.15.0 is installed on
# PYTHON_SINGLE_TARGET=python3_14, and 4.57.6 only supports python3_{12,13}, so
# the downgrade cannot satisfy REQUIRED_USE and aborts any @installed pass
# ("update-seed failed", 2026-08-20). The installed 0.2.3 has no such cap and
# works. Keep it until upstream supports transformers 5 - then drop this mask
# rather than assume a newer version is safe, because a bump that keeps the cap
# would resurrect exactly this failure.
>=dev-python/xgrammar-0.2.5
