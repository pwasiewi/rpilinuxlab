# Kernel hardening for the amd64kdehardened live image.
# Copied verbatim into the image by 'xstage amd64 conf' (PROF_LIVE_OVERLAY_DIR).
# Source and rationale: e/info/intro-hardening.md.
#
# Deliberately excluded here: anything that needs per-host tuning (network
# tuning, IMA policy, TPM/PCR bindings) — a live image boots on unknown
# hardware, so only settings that are safe everywhere belong in this file.

# --- kernel surface --------------------------------------------------------
kernel.kptr_restrict = 2
kernel.dmesg_restrict = 1
kernel.perf_event_paranoid = 3
kernel.kexec_load_disabled = 1
kernel.unprivileged_bpf_disabled = 1
kernel.yama.ptrace_scope = 1
# core dumps from setuid binaries never reach disk
fs.suid_dumpable = 0

# --- filesystem link protections ------------------------------------------
fs.protected_symlinks = 1
fs.protected_hardlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2

# --- network ---------------------------------------------------------------
net.core.bpf_jit_harden = 2
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.all.accept_source_route = 0
net.ipv4.tcp_syncookies = 1
net.ipv6.conf.all.accept_redirects = 0
net.ipv6.conf.all.accept_source_route = 0
