# Version locks for the CUDA/ML stack shipped by the nvidia-flavoured images.
#
# WHY THIS FILE EXISTS: 'pkgs' runs
#     emerge --update --deep --newuse @world @installed        (xstage:1757)
# so every run re-proposes the newest ::stuff versions of the ML packages.
# dev-python/vllm-9999 (::pwr) pins several of them to an exact version, and a
# newer sibling cannot share slot 0 with the pinned one — the result is a wall
# of "slot conflict" and emerge merging NOTHING AT ALL ("Nothing to merge;
# quitting"), i.e. the whole image update stalls rather than degrading.
#
# Note the asymmetry that makes this confusing: the "skipped due to unsatisfied
# dependencies" notices (xgrammar, openai, mcp) are harmless chatter; only the
# slot conflicts below actually stop the merge.
#
# Each pin names the package that imposes it, so a future bump knows what has to
# move first. Lifting any of these means rebuilding vLLM (hours) against the new
# API — see the pin at vllm-9999.ebuild:143 and memory project_pwr_overlay.

# --- pinned by dev-python/vllm-9999 (~exact-version deps in the ebuild) -------
>dev-python/tilelang-0.1.9
>dev-python/humming-kernels-0.1.4
>dev-python/nvidia-cutlass-dsl-4.5.2
# the CUTLASS sub-packages move in lockstep with the parent; 4.6.x also split
# out a NEW one (nvidia-cutlass-dsl-libs-core) that does not exist at 4.5.2
>dev-python/nvidia-cutlass-dsl-libs-base-4.5.2
>dev-python/nvidia-cutlass-dsl-libs-cu13-4.5.2

# --- flashinfer: python and cubin halves must match exactly ------------------
# flashinfer-python-0.6.17 requires ~flashinfer-cubin-0.6.17 (vllm pin 7a100bb617); 0.6.18 would
# drag in CUTLASS 4.6.2, which the vLLM pin above forbids.
>=dev-python/flashinfer-python-0.6.18
>=dev-python/flashinfer-cubin-0.6.18

# --- pinned by other installed packages, not by vLLM -------------------------
# dev-python/model-hosting-container-standards needs <dev-python/fastapi-0.137.0
>=dev-python/fastapi-0.137.0
# tokenizers follows transformers in lockstep: 5.15 needed =0.22*, 5.17 needs
# >=0.23.1 <0.24 (2026-09-21: the wall at 0.23 made @installed die on
# "tokenizers-0.23.2 masked" once ::stuff offered transformers-5.17). Nothing
# else bounds transformers from above (vllm >=5.10.4, trl >=4.56.2, unsloth/peft
# unbounded), so the pin only has to stop tokenizers running AHEAD of it.
>=sci-ml/tokenizers-0.24

# quack-kernels 0.6.x ALL require cutlass >=4.6.0 (0.6.1->4.6.0, 0.6.2/0.6.3->
# 4.6.1, 0.6.4->4.6.2), so the entire 0.6 line is unreachable while the vLLM
# pin above holds 4.5.2. Without this, @installed proposes 0.6.4 every run and
# dies on "nvidia-cutlass-dsl-4.6.2 masked" — the failure looks like a CUTLASS
# problem but the package that moved is this one. Installed: 0.5.0, which asks
# for ~4.5.2 and matches the wall exactly.
>dev-python/quack-kernels-0.5.0

# nvidia-cudnn-frontend-1.29.0 (::stuff, 2026-09) requires >=nvidia-cutlass-dsl-4.6.2,
# which the vLLM pin above forbids — same shape as quack-kernels: @installed
# proposes 1.29.0 every run and update-seed dies on "nvidia-cutlass-dsl-4.6.2
# masked" (2026-09-21, 16 min in). Installed: 1.28.0; its only consumer,
# flashinfer-python-0.6.17, asks for >=1.25.0.
>dev-python/nvidia-cudnn-frontend-1.28.0

# --- ::stuff ebuilds that cannot merge (2026-09-21, seen on the host 09-20 too) ---
# cuda-bindings-13.4.1: the GitHub tag tarball no longer matches ::stuff's
# Manifest (3267640 bytes fetched vs 3267635 recorded — GitHub regenerated the
# archive), so the fetch ends in a checksum failure; cuda-python-13.4.1 pins
# ~cuda-bindings-13.4.1 and falls with it.
# The rest of the CUDA 13.4 wave is held back anyway by xgrammar's
# nvidia-cuda-toolkit:0/13.3.1= slot dep, so keep the python halves at 13.3 too.
>=dev-python/cuda-bindings-13.4
>=dev-python/cuda-python-13.4
# cuda-tile-bin-1.6.0: python_install runs `installer` once per PYTHON_COMPAT impl
# and the wheel ships a console script (usr/bin/cutile-cache) — the second impl
# dies with FileExistsError. Ebuild bug; 1.5.0 installs and satisfies the
# cutlass-dsl-libs dependency.
>dev-python/cuda-tile-bin-1.5.0

# dev-python/wxpython-4.3.1::stuff requires ~dev-python/sip-6.12.0, which is
# OLDER than the installed sip-6.15.3 (::gentoo) and is not keyworded — so the
# "update" is a downgrade of a dependency that pyqt6 also uses. ::gentoo tops
# out at 4.2.5, which is what is installed and what nothing in this image
# depends on anyway. Mask the ::stuff version rather than keyword an old sip.
>=dev-python/wxpython-4.3.1
