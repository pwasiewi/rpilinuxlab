# Kconfig fragment: kernel-side hardening for the amdkdehard profile.
# Merged AFTER live.fragment, so where the two disagree this one wins.
#
# WHY THIS FILE EXISTS AT ALL: the 'hardened' portage profile is a USERLAND
# setting — it changes the toolchain (PIE, SSP, FORTIFY_SOURCE, RELRO, BIND_NOW)
# and rebuilds userspace with it. It does not touch the kernel. A stage3 built
# on default/linux/amd64/23.0/hardened therefore boots an ordinary kernel unless
# something like this fragment is applied. Answering the obvious question: yes,
# the hardened stage3 needs this, and no, it does not come for free.
#
# The options below are the KSPP (Kernel Self Protection Project) recommended
# settings, minus the ones that would make a rescue image unusable — see the
# 'deliberately NOT set' note at the bottom.

# --- memory corruption defences ---------------------------------------------
CONFIG_HARDENED_USERCOPY=y
CONFIG_FORTIFY_SOURCE=y
CONFIG_STACKPROTECTOR=y
CONFIG_STACKPROTECTOR_STRONG=y
CONFIG_VMAP_STACK=y
CONFIG_THREAD_INFO_IN_TASK=y
CONFIG_SCHED_STACK_END_CHECK=y
CONFIG_SHUFFLE_PAGE_ALLOCATOR=y
CONFIG_SLAB_FREELIST_RANDOM=y
CONFIG_SLAB_FREELIST_HARDENED=y
CONFIG_RANDOM_KMALLOC_CACHES=y
CONFIG_INIT_ON_ALLOC_DEFAULT_ON=y
CONFIG_INIT_ON_FREE_DEFAULT_ON=y
CONFIG_ZERO_CALL_USED_REGS=y
CONFIG_BUG_ON_DATA_CORRUPTION=y
CONFIG_LIST_HARDENED=y
CONFIG_DEBUG_LIST=y
CONFIG_DEBUG_SG=y
CONFIG_DEBUG_CREDENTIALS=y
CONFIG_DEBUG_NOTIFIERS=y
CONFIG_DEBUG_VIRTUAL=y

# --- address-space layout ---------------------------------------------------
# KASLR: notably OFF in this host's zen config — an image that calls itself
# hardened must not inherit that.
CONFIG_RANDOMIZE_BASE=y
CONFIG_RANDOMIZE_MEMORY=y
CONFIG_RELOCATABLE=y

# --- read-only / write-execute separation ------------------------------------
CONFIG_STRICT_KERNEL_RWX=y
CONFIG_STRICT_MODULE_RWX=y
CONFIG_DEBUG_WX=y
# STATIC_USERMODEHELPER freezes modprobe_path and friends into read-only
# kernel data: the ONLY binary the kernel ever execs is
# /sbin/usermode-helper, which re-execs an audited whitelist (source:
# live/kernel/usermode-helper.c, compiled into the rootfs by
# cmd_live_kernel). Without that binary this option silently kills ALL
# kernel-initiated module autoloading — mount says "unknown filesystem
# type 'binfmt_misc'", iptables says "Table does not exist" (found on the
# installed nvhard host, 2026-08-10). The build dies if the pair splits.
CONFIG_STATIC_USERMODEHELPER=y

# --- structure layout randomisation (needs the gcc plugin) -------------------
CONFIG_GCC_PLUGINS=y
CONFIG_GCC_PLUGIN_LATENT_ENTROPY=y
CONFIG_RANDSTRUCT_FULL=y

# --- CPU-level mitigations and isolation ------------------------------------
CONFIG_PAGE_TABLE_ISOLATION=y
CONFIG_RETPOLINE=y
CONFIG_CPU_MITIGATIONS=y
CONFIG_X86_KERNEL_IBT=y
CONFIG_X86_USER_SHADOW_STACK=y
# kCFI — TRIED 2026-08-26, REVERTED THE SAME DAY. Do not re-enable without
# reading this, and do not trust the check that fooled us.
#
# CONFIG_CFI=y booted fine on intelhard (bare QEMU: "SMP alternatives: CFI:
# Using rehashed retpoline kCFI", clean init, no panics) and the out-of-tree
# modules looked correct everywhere — nvidia.ko carried 18 __kcfi_typeid
# symbols, nvidia-drm.ko 56, vboxdrv.ko 263. On that evidence the kernel was
# transferred to the nvhard host and it HUNG ON BOOT, twice. Recovered via the
# 7.1.5 fallback entry.
#
# THE LESSON, which is the reason this comment is long: presence of
# __kcfi_typeid proves only that the module was COMPILED with -fsanitize=kcfi.
# It says nothing about whether the driver's indirect calls actually satisfy
# the type checks at runtime. The proprietary NVIDIA stack casts function
# pointers across incompatible prototypes; that compiles, instruments, loads —
# and then trips kCFI on the first such call. A static symbol count cannot see
# this. Only a real boot on real hardware can, which is exactly the test the
# Intel profiles could not perform because they have no NVIDIA GPU.
#
# So "it works on intelhard" was never transferable evidence, and keeping the
# option enabled only where there is no NVIDIA would leave the fleet split
# between images that differ in a kernel-level security property — a worse
# state than not having it at all (operator decision).
#
# If this is ever revisited: the workable shape is CONFIG_CFI=y together with
# the nouveau/open-kernel-module path, or waiting for NVIDIA to declare kCFI
# support. CONFIG_CFI_PERMISSIVE is NOT the answer — it downgrades violations
# to warnings, which means shipping a mitigation that does not mitigate.
# CONFIG_CFI=y
CONFIG_SPECULATION_MITIGATIONS=y
CONFIG_IOMMU_SUPPORT=y
CONFIG_INTEL_IOMMU=y
CONFIG_INTEL_IOMMU_DEFAULT_ON=y
CONFIG_AMD_IOMMU=y
CONFIG_IOMMU_DEFAULT_DMA_STRICT=y

# --- LSM stack ---------------------------------------------------------------
CONFIG_SECURITY=y
CONFIG_SECURITY_YAMA=y
CONFIG_SECURITY_LOCKDOWN_LSM=y
CONFIG_SECURITY_LOCKDOWN_LSM_EARLY=y
CONFIG_SECURITY_DMESG_RESTRICT=y
CONFIG_SECURITY_NETWORK=y
CONFIG_SECURITY_SAFESETID=y
CONFIG_LSM="landlock,lockdown,yama,integrity,bpf"
CONFIG_SECURITY_LANDLOCK=y
# 'bpf' in CONFIG_LSM is inert without the LSM itself — olddefconfig defaults
# it to n, and systemd's pre-merge check flags the miss (it wants it for
# RestrictNetworkInterfaces= and friends)
CONFIG_BPF_LSM=y
CONFIG_BPF_UNPRIV_DEFAULT_OFF=y

# --- legacy interfaces that are pure attack surface -------------------------
# CONFIG_DEVMEM is not set
# CONFIG_DEVKMEM is not set
# CONFIG_DEVPORT is not set
# CONFIG_PROC_KCORE is not set
# CONFIG_LEGACY_PTYS is not set
# CONFIG_LEGACY_TIOCSTI is not set
# CONFIG_COMPAT_BRK is not set
# CONFIG_COMPAT_VDSO is not set
# CONFIG_VIVID is not set
# CONFIG_ACPI_CUSTOM_METHOD is not set
# CONFIG_X86_MSR is not set
#   ^ this one has a cost: sys-apps/msr-tools ships in the package list and
#     stops working, as does anything reading /dev/cpu/*/msr (some undervolting
#     and thermal tools). Drop the line if you need them more than you need the
#     write-anything-to-a-model-specific-register primitive gone.

# --- deliberately NOT set ---------------------------------------------------
# CONFIG_MODULE_SIG_FORCE: signing needs a key, and a key baked into a public
#   ISO signs nothing meaningful. Enable it for images you build and keep.
# CONFIG_IA32_EMULATION off: it is off by default on modern configs, but forcing
#   it here breaks 32-bit rescue binaries people actually carry on live media.
# CONFIG_KFENCE / KASAN: sampling allocators cost real performance and this is
#   a workstation image, not a fuzzing target.
# CONFIG_MODULES off: a rescue image loads drivers for hardware it has not met.
